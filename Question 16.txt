//16.Find the roots of a quadratic equation.
//(ax^2 + bx + c = 0)
#include <stdio.h>
#include <math.h>
int main() 
{
  float a,b,c;
  printf("enter the value of a=");
  scanf("%f",&a);
  printf("enter the value of b=");
  scanf("%f",&b);
  printf("enter the value of c=");
  scanf("%f",&c);
  float discriminant=(b*b)-(4*a*c);
  float r1=(-b+(sqrt)(discriminant))/(2*a);
  float r2=(-b-(sqrt)(discriminant))/(2*a);
  printf("%f is a root of quadratic equation=\n",r1);
  printf("%f is a root of quadratic equation=",r2);
  return 0;
}